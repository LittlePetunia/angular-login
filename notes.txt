Still getting overwhelmed.
Todo:
- Create welcome page with link to login, register.
- Create registration page that
  - submits new user info to db


When that is done we can deal with logging in



Am getting overwhelmed with all i need to do eventually like role based menu.
For now keep it simple and evolve it.
What is the bare minimum I need to have:
- welcome page
- log in page/modal
- registration page
- home page (for logged in user)
  - with user information displayed

DB: User{
   userId
  ,userName
  ,password
  ,session?/token?
  ,firstName
  ,lastName
  }


partials{
  welcome.html
  ,loginModal.html
  ,registration.html
  ,home.html
}

modules
  app{
    currentUser
  }
  .config(
  routes{
    /
    ,welcome
    ,login
    ,register
    ,home
  });
}

service
  // this might need to be separated into authenticationSvc and userSvc

  authSvc{
    // returns true if user info is valid(user/pass)
    authenticate(userInfo): sessionId

    // returns true if user session is valid
    ,isAuthenticated(session):
  }
}
  userSvc{
     create(): user
    ,edit(): user
    }
}





-------------------
use https-only cookeies to store access tokens.

will still be vulnerable to csrf attacks so need a crsf token strategy.
  https://www.owasp.org/index.php/Cross-Site_Request_Forgery_%28CSRF%29_Prevention_Cheat_Sheet#CSRF_Prevention_without_a_Synchronizer_Token
  https://www.owasp.org/index.php/Cross-Site_Request_Forgery_%28CSRF%29_Prevention_Cheat_Sheet


https://github.com/auth0/node-jsonwebtoken
